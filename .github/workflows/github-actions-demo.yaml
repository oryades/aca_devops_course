name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    uses: docker://node:16-alpine
    steps:
      - run: npm install
      - run: npm run build
    with:
      name: artifact
      path: |
        dist

